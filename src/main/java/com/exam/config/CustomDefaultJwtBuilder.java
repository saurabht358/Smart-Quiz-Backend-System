package com.exam.config;



public class CustomDefaultJwtBuilder  {


//    public  JwtBuilder signWiths(SignatureAlgorithm alg, String base64EncodedSecretKey) {
//        System.out.println("custome signature used");
//        Assert.hasText(base64EncodedSecretKey, "base64-encoded secret key cannot be null or empty.");
//        Assert.isTrue(alg.isHmac(), "Base64-encoded key bytes may only be specified for HMAC signatures.  If using RSA or Elliptic Curve, use the signWith(SignatureAlgorithm, Key) method instead.");
//        byte[] bytes = Base64Ex.decode(base64EncodedSecretKey);
//        return signWith(alg, bytes);
//    }
}
